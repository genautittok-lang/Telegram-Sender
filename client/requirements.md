## Packages
framer-motion | Smooth layout transitions and entry animations
recharts | Visualizing message statistics and activity
date-fns | Formatting timestamps and durations
clsx | Utility for constructing className strings conditionally (often paired with tailwind-merge)
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["Inter", "sans-serif"],
  mono: ["JetBrains Mono", "monospace"],
}
Dark mode is forced by default in App.tsx.
API endpoints correspond to @shared/routes structure.
